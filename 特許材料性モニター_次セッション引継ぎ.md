# 特許材料性モニター 次セッション引継ぎ

以下は、新しいCodex会話へそのまま貼り付けるための引継ぎです。

---

## 依頼の背景と最終目標

私は、上場している先端技術関連の日本企業を対象に、新着特許が株価材料になり得るかを毎日自動判定する「特許材料性モニター」を開発しています。

最終的に実現したい流れは次のとおりです。

1. EPO OPSなどから新着特許を毎日取得する
2. 重複・特許ファミリーを統合する
3. 出願人を監視対象の上場企業へ名寄せする
4. IPC/CPC、タイトル、要約、請求項などで安価な事前評価を行う
5. 過去特許との差から新規性・異常度を評価する
6. 企業規模を加味して株価材料性を評価する
7. 候補をGPT-5 nanoで一次審査する
8. 一次審査の上位候補だけGeminiで再審査する
9. 重要特許を1日1回メールで通知し、本当に重大な特許は即時通知する
10. yfinance等で公開後の株価を追跡して答え合わせし、判定精度を継続改善する
11. GitHub Actionsで1日1回自動実行し、Supabaseに状態を保存する

PCを常時起動せずに運用すること、API費用を抑えること、途中で処理が打ち切られても翌日に別順序で挽回できることを重視しています。

## 作業場所

メインの作業フォルダ:

`C:\Users\saban\Documents\Codex\2026-07-03\gem`

主要ファイル:

- GUI: `patent_monitor_tool.py`
- 日次自動実行: `patent_monitor_daily.py`
- 中核処理: `patent_monitor\pipeline.py`
- 通知: `patent_monitor\notifications.py`
- 株価の答え合わせ: `patent_monitor\market_feedback.py`
- Supabase連携: `patent_monitor\supabase_state.py`
- 資格情報保存: `patent_monitor\secure_store.py`
- 設定: `patent_monitor_config.json`
- 企業マスタ: `patent_company_master.csv`
- テスト: `test_patent_monitor.py`
- モック検証: `validate_patent_monitor_mock.py`
- GUI起動: `launch_patent_monitor.bat`
- GitHub Actions配布用: `github_patent_monitor_ready`
- GitHub用再生成: `package_patent_monitor_github.py`

過去の出力:

`C:\Users\saban\Documents\Codex\2026-07-03\gem\outputs\patent_monitor`

## 現在のバージョンと直近の状況

現在の中核バージョンは **v0.8.0** です。

直前の実データ出力では、2026-07-14～15のEPO特許991ファミリーに対して次の状態でした。

- 全ファミリー: 991件
- 企業名寄せ成功: 7件
- GPT一次審査: 2件
- Gemini再審査: 0件
- 未一致: 984件
- IPC/CPCが全991件で空欄

GPT件数が少なかった主因はAPI上限ではなく、上流のデータ品質でした。

- EPO検索結果だけを使い、詳細書誌・要約・IPC/CPCを取得していなかった
- EPO出願人名の `[JP]`、英語法人表記、子会社名を十分に正規化できていなかった
- 履歴が少ない企業のpercentileが極端になり、候補判定が処理順にも影響されていた
- 技術適合度がほぼ0になり、GPT送信閾値へ届かなかった

## v0.8.0で実施済みの修正

### EPOデータ取得

- EPO OPSの検索結果取得後、監視企業に一致しそうな特許だけ詳細取得する処理を追加
- bibliographic/abstract endpointから英語タイトル、英語要約、請求項、IPC、CPCを補完
- kind codeあり・なしのフォールバック、リトライ、個別エラー継続を追加
- 全991件の詳細を取らず、名寄せ候補だけを補完するためAPI負荷を抑制
- `ops_enrich_details=true`、`ops_detail_max_records=200` を設定済み

### 企業名寄せ

- `[JP]` などの国コードを除去
- `KABUSHIKI KAISHA` 等を正規化
- 英語表記・子会社名を企業マスタに追加
- 住友ゴム工業を追加
- NTT DOCOMO、Canon Medical Systems、Kyocera Document Solutions、Shiseido等の別名を追加
- 企業マスタは現在451社
- 過去991件を再マッチした簡易検証では、企業一致が7件から14件へ増加

### percentile判定

- 企業履歴5件以上なら企業内percentile
- 企業履歴が少なくても全体履歴30件以上なら全体percentile
- それも不足なら中立値50
- 同一run内の処理順でpercentileが変わらないよう、run開始前の履歴を固定
- CSVへ `percentile_basis` と `percentile_sample_count` を追加

### 診断性

- CSVへ `epo_detail_enriched` と `epo_detail_errors` を追加
- `run_summary.json` に詳細取得件数、分類取得件数、要約取得件数、名寄せ件数、中立percentile件数等を追加

### 配布用同期

- `package_patent_monitor_github.py` を実行し、`github_patent_monitor_ready` もv0.8.0へ同期済み

## 現在のAI審査設定

設定ファイルの主要値:

- GPTモデル: `gpt-5-nano`
- GPT月間上限: 500件
- 一次候補閾値: 68
- Geminiモデル: `gemini-3.1-flash-lite`
- Gemini再審査閾値: 85
- Gemini月間上限: 500件
- Gemini昇格率の目標: GPT送信件数のおよそ10%
- Gemini昇格閾値は履歴を見て少しずつ自動調整
- 企業内percentile閾値: 95
- 棄却監査: 5%

処理の基本順序は、安価なルール評価 -> GPT-5 nano -> 重要候補のみGeminiです。

## 通知と自動運用

実装済みの想定:

- 1日1回のダイジェスト通知
- 本当に重大な特許のみ即時通知
- 即時通知対象はダイジェスト対象の1%以下を目標
- GitHub Actionsで日次実行
- Supabaseへ履歴・状態を同期
- 探索順を日ごとに変え、途中終了時の偏りを翌日以降に補う
- yfinanceによる公開後株価の追跡とフィードバック蓄積

ただし、新しい会話ではコードを読み、これらが「完全実装済み」「部分実装」「設定だけ」のどれかを再確認してから変更してください。会話上の構想と実装状態を混同しないでください。

## 出力ファイルの意味

各runフォルダには主に次が出ます。

- `patent_evaluations.csv`: 全候補のルール評価、AI評価、名寄せ結果
- `gpt_reviewed_or_pending.csv`: GPT一次審査済みまたは候補
- `gemini_reviewed_or_pending.csv`: Gemini再審査済みまたは候補
- `company_match_review.csv`: 名寄せ要確認
- `run_summary.json`: 件数、モデル、エラー、品質診断

## テスト済み事項

- `python -m unittest test_patent_monitor.py`: 24件成功
- `validate_patent_monitor_mock.py`: 成功
- モックHTTPでEPO詳細取得、英語タイトル、IPC/CPC抽出を確認
- 過去991件への新名寄せで7件から14件へ改善

## 未確認・次に最優先で行うこと

### 1. 新版でEPO実データを再実行

同程度の日付範囲・最大取得件数でv0.8.0を実行し、旧runと比較してください。

確認する数値:

- `detail_enriched_count`
- IPC/CPC充足件数・率
- abstract充足件数・率
- matched_count
- GPT送信件数
- Gemini送信件数
- EPO 403/429/404等の詳細エラー内訳

期待する変化:

- IPC/CPCが全件空欄ではなくなる
- 名寄せが7件より増える
- 技術適合度0件が減る
- GPT送信件数が自然に増える

### 2. 送信件数を見て閾値を再調整

上流データが直る前に閾値だけ下げるのは避けてください。新版の実runを見てから、GPTの費用と見逃し率のバランスを決めます。

### 3. EPO詳細取得の実通信確認

Codex環境からはWindows DPAPIで保存済みEPO資格情報を復号できず、実EPO詳細APIだけは直接検証できませんでした。ユーザーの通常WindowsセッションでGUIから実行して確認してください。これはコード不良を示すものではなく、Codex実行環境のユーザー分離によるものです。

### 4. EPOのAPI制限を監視

詳細取得を追加したため、429や403が増えないか確認してください。必要なら次を実装・調整します。

- 詳細取得キャッシュ
- resume/checkpoint
- 指数バックオフ
- 翌日再試行キュー
- 日次上限による打ち切りと持越し

## APIキーとセキュリティ

APIキーそのものはこの引継ぎに記載しません。

過去の会話・スクリーンショットでOpenAIキーやEPO Consumer Key/Secretが表示されたことがあります。漏えい扱いにしてローテーションを推奨します。新しいCodexは、チャットに貼られたキーをコード、JSON、CSV、ログ、Gitへ保存しないでください。

資格情報は次のどちらかを使います。

- GUIの「Windowsユーザー用に暗号化して保存」
- GitHub Actions Secrets / 環境変数

想定環境変数:

- `OPENAI_API_KEY`
- `GEMINI_API_KEY`
- `EPO_OPS_KEY`
- `EPO_OPS_SECRET`
- Supabase・SMTP関連のSecrets

## コーディング時の注意

- 既存ファイルを先に読み、v0.8.0の修正を消さない
- 変更時はアプリ右上のバージョンも上げる
- GUI版と日次版の両方へ同じ修正を反映する
- 変更後に `package_patent_monitor_github.py` でGitHub用フォルダを同期する
- テストとモック検証を必ず実行する
- PowerShellの既定表示ではUTF-8日本語が文字化けすることがある。ファイル破損と即断せず、PythonまたはUTF-8指定で読む
- GPT/Geminiの送信件数だけでなく、上流の名寄せ率・分類充足率を必ず診断する
- 見逃し削減を重視するが、全件をAIへ送らず、ルール評価とキャッシュで費用を抑える

## 新しいCodexへの最初の依頼

まずv0.8.0のコードと直近runを確認してください。その後、EPO実データを使った新しいrunの `run_summary.json` と各CSVを旧runと比較し、詳細取得、IPC/CPC充足、企業名寄せ、GPT送信件数が改善したかを定量評価してください。不具合があれば原因を切り分けて修正し、テスト、モック検証、GitHub配布用同期まで実施してください。

---

