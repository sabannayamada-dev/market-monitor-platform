# 時価総額帯別・固定閾値底検知モデル比較

既存の `app.py` は変更せず、同じ株価キャッシュDBを使って、小型株・中型株・大型株向けの固定閾値モデルを比較する解析専用アプリです。回帰分析や機械学習は使いません。

## 起動

`launch_market_cap_model_analysis_tool.bat` をダブルクリックします。

Anaconda Promptから起動する場合:

```powershell
cd C:\Users\saban\Documents\Codex\2026-07-03\gem
python market_cap_model_analysis_tool.py
```

## 比較するモデル

- `JP-MID-BTM-v1`: 現行 `app.py` の v5.3設定を固定した比較基準
- `JP-SMALL-BTM-v0`: 小型株向けの暫定固定閾値
- `JP-LARGE-BTM-v0`: 大型株向けの暫定固定閾値

小型株・大型株の初期値は仮説です。まず全銘柄へ3モデルを同時適用し、期間別成績、偽底率、大化け率、ベンチマーク超過、上位銘柄除外後の成績で比較します。中型株モデルは比較基準を動かさないため画面上で編集できません。

## 時価総額境界セット

操作画面の `時価総額境界セット` には、次の書式で複数セットを指定できます。

```text
セット名:小型上限億円:大型下限億円;セット名:小型上限億円:大型下限億円
```

初期値は次の3セットです。

- `標準:700:5000`
- `中型1100-3300:1100:3300`
- `中型広め:500:7000`

各企業・各モデルの底検知は一度だけ計算し、その結果へ全境界セットをまとめて適用します。セット数を増やしても株価探索や底検知は再実行しないため、追加時間はCSV集計分のみです。1番目のセットは従来の `時価総額区分` と `自動振分一致` に使う基準セットにもなります。

解析完了後に境界だけ変更した場合は、`境界セットだけ再集計` を押してください。既存の `model_events.csv` を再利用するため、底検知をやり直さず数秒以内で更新できます。

## 時価総額データ

時価総額CSVは任意です。未指定でも3モデルの横並び比較はできます。添付された現在時価総額データは `data/current_market_cap_snapshot.csv` として同梱され、操作画面の初期値に設定されています。

時価総額帯へ自動振分する場合は `market_cap_history_template.csv` と同じ列を持つCSVを指定します。

- `銘柄`: 例 `7203.T`
- `日付`: その時点の時価総額の日付。イベント時点以前で直近の行を使用
- `時価総額円`: 円単位
- `時価総額区分`: 任意。`small`、`mid`、`large`

日付がない静的な現在時価総額も読めますが、過去イベントの分類に使うと未来情報の混入になります。出力では `時価総額情報種別=static_lookahead_risk` と記録します。

### 現在時価総額からの推定

CSVに `現在時価総額(億円)`、`現在価格`、`現在日` がある場合は、次の式でシグナル時点を推定します。

```text
シグナル時推定時価総額
= 現在時価総額 × シグナル価格 ÷ 現在価格
```

出力には `現在時価総額円`、`時価総額基準日`、`時価総額基準価格`、`時価総額推定倍率` を残します。情報種別は `estimated_from_current_market_cap` です。

この方式は株式分割には概ね対応しますが、基準日からシグナル日までに増資、自社株買い、合併などで発行済株式数が変化した場合は誤差が生じます。正確なpoint-in-time発行済株式数が得られた場合は、そちらを優先してください。

## 主な出力

- `model_events.csv`: シグナル単位の全明細。将来リターン、最大上昇・下落、ベンチマーク超過など
- `model_summary.csv`: モデル別・年別・相場期間別の平均、中央値、勝率、偽底率、大化け率
- `top_winner_exclusion.csv`: 上位1、3、5、10イベントを除いた感応度。少数の大化け依存を確認
- `model_signal_overlap.csv`: 同じ銘柄・同じ日に、どのモデルがシグナルを出したか
- `benchmark_equal_weight_summary.csv`: 各シグナル日に個別株、ACWI、SPYへ同額投資して現在まで保有した場合のモデル別平均・中央値・超過率
- `market_cap_band_model_summary.csv`: small / mid / largeの各帯の中で3モデルを横並び比較
- `routed_model_summary.csv`: 時価総額帯に応じて小型・中型・大型モデルを自動選択したイベントだけの成績
- `market_cap_coverage_summary.csv`: 時価総額推定の取得率と規模帯別件数
- `threshold_set_routed_events.csv`: 各境界セットで実際に採用されたモデル・シグナルの全明細
- `threshold_set_summary.csv`: 境界セットごとの総合成績と小型・中型・大型モデル採用数
- `threshold_set_model_summary.csv`: 境界セット内のモデル別成績内訳
- `model_settings.json`: 使用した全閾値、既存 `app.py` のSHA-256、実行条件
- `analysis_report.json`: 処理件数、所要時間、停止状態、出力先
- `analysis_state.json` / `events_checkpoint.csv`: 中断・再開用

## 推奨手順

1. まず時価総額CSVなし、3モデル全適用で実行する。
2. `model_summary.csv` の中央値、偽底率、ベンチマーク超過を優先して比較する。
3. `top_winner_exclusion.csv` で一部の大化け銘柄だけに依存していないか確認する。
4. point-in-time時価総額を用意できたら、自動振分の成績を確認する。
5. 小型・大型モデルの閾値は、一度に一項目だけ動かして再検証する。

`続きから開始` は同じ出力フォルダと同じ設定でのみ動作します。設定が異なる場合は、誤混合を防ぐため再開を拒否します。

## 同額購入比較

画面の「同額購入比較（カンマ区切り）」へ `ACWI,SPY` のように入力します。シグナルごとに同額を投資し、各銘柄を現在まで保有したものとして単純平均します。同時保有数や資金上限を考慮したポートフォリオ・バックテストではありません。

ACWIとSPYはUSD建て、対象日本株はJPY建てです。現状の比較は各通貨内の騰落率比較であり、円換算時の為替損益は含みません。
