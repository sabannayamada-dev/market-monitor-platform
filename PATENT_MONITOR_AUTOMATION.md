# 特許材料性モニター 自動運転

## 初回設定

1. `launch_patent_monitor.bat` からアプリを起動する。
2. 「詳細設定」でOpenAI、Gemini、EPO OPSの接続確認を行う。
3. 「自動実行・通知」でSMTP情報を設定する。
4. Gmailの場合は通常のパスワードではなく、Googleのアプリパスワードを入力する。
5. 「設定と資格情報を保存」を押す。
6. 「テストメール送信」で受信を確認する。
7. 実行時刻を指定し、「毎日自動実行を登録」を押す。

## 毎日の処理

1. 直近3日間を対象に、監視企業ごとにEPO OPSを検索する。
2. 特許ファミリー統合、企業名寄せ、ローカル事前評価を行う。
3. GPT-5 nanoで一次審査し、約10%をGeminiへ送る。
4. `important` または `urgent` の未通知特許だけをメールする。
5. 成熟した過去特許の株価をyfinanceで答え合わせする。
6. 60取引日後のTOPIX超過収益モデルを再学習する。
7. ログと状態を `outputs/patent_monitor/daily_logs` に保存する。

## 安全設計

- APIキーとメール用アプリパスワードはWindows DPAPIで暗号化する。
- 同じ公開番号はメール送信履歴をSQLiteへ保存し、二重送信しない。
- 3日重複探索により、休日や一時的な失敗を翌日以降に回収する。
- 二重起動をロックファイルで防止する。
- 株価取得に失敗しても、特許評価結果は保持する。
- 日次処理そのものが失敗した場合はエラーメールを送る。

## 注意

- PCが電源OFFの場合は実行されない。
- EPO OPSアカウントがblockedの間は自動収集できない。
- 学習モデルは60日後の結果が30件以上蓄積してから作成される。
- 予測値は将来を保証せず、通知順位を補助するための統計値である。
