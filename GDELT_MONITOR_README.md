# GDELTニュース材料性モニター

## 現行運用（Web NGrams方式）

- DOC APIはVPS IPから最小要求でもHTTP 429になったため、通常運用では使用しない。
- GDELT Web NGramsの静的ファイルを、固定120分ごとに差分取得する。
- 探索窓は150分とし、前回周期との重複30分で遅延公開を拾う。`ngram_files`に記録済みの本文ファイルは再取得しない。
- HEAD探索は0.1秒間隔、本文ファイルの取得は2秒間隔とし、短時間に集中アクセスしない。
- TOCはローカルへ72時間キャッシュし、15分速報監視と通常収集で共有する。
- 通常ニュースは本文NGrams上で「材料語＋安全な企業別名」をローカル判定し、候補だけをDB・AI・メールへ渡す。

### 超重要速報

- 既存の2分systemdハートビート内で、15分ごとにTOCだけを軽量確認する。
- 対象は主要海峡・運河の閉鎖/封鎖、再開/解除、正式な宣戦布告。
- `may`、`could`、`warns`、`検討`、`可能性`などの未発生表現は除外する。
- 信頼媒体1件、または異なるドメインかつ異なるタイトル2件で確認した場合だけ速報候補にする。
- 同一イベント・同一状態は6時間再通知しない。閉鎖と再開は別状態として通知できる。
- 速報判定はローカルの決定論的ルールで行い、AIを判定条件にしない。
- 速報メールはタイトル判定であることを明記し、記事本文・公的発表の確認を促す。

## 目的

世界ニュースを大量に読むのではなく、日本株に影響し得る異変の候補を低コストで絞り、
収集自体が正常だったかを毎回監査できる形で保存する。

## 4段階の運用

運用段階は `GDELT_OPERATION_STAGE` だけで切り替える。複数の真偽値を組み合わせず、
意図しないメール送信を防ぐ。

1. 収集：GDELT取得、重複排除、DB保存、通信診断。
2. ルール選別：企業名照合、材料キーワード、決定論的スコアリング。
3. AI影運用：GPT一次審査、重要候補のみGemini二次審査。結果はDB保存のみ。
4. 通知運用：日次メールと診断フッターを有効化。

第3段階の初期上限はGPT 3件/日、Gemini 1件/日とし、品質と費用を確認後に増やす。
第1～3段階では、`--force-email` を指定してもメールは送信しない。

## 初期運用

- systemdは2分ごとに軽量起動し、SQLiteに保存した次回収集時刻に達した場合だけGDELTへ接続する。
- 広域クエリ4本、90分の重複窓、1クエリ最大25件。
- クエリ間隔zの初期値は20秒。
- `GDELT_OPERATION_STAGE=1` で影運用を開始する。
- 403/429を検知したらその場で停止する。429では適応制御x・yを更新する。
- 有効なJSONで記事0件の場合と、空・HTML・非JSON応答を区別する。

## 保存する診断値

予定・実行・成功クエリ、0件応答、非JSON、403、429、タイムアウト、その他エラー、
探索記事、新規、重複、企業一致、掲載基準通過、GPT/Gemini送信、最終候補、持越しを
`GDELT_STATE_DB` の `collection_runs` に保存する。メールを有効化した後は同じ値を本文末尾へ載せる。

## 適応制御

- 収集周期xは初期60分、最小30分、最大180分。
- 429を受けるたびに `x = min(x + 4, 180)`。
- 24時間429がなく、その間に6回以上完全成功した場合、一度だけ `x = max(x - 2, 30)`。
- 休止時間yは1時間固定。回復試行の成否で増減させない。
- 既存DBに1時間を超える休止が残っている場合、起動時に残り時間を最大1時間へ短縮する。
- `Retry-After` が返された場合、実休止時間はyとRetry-Afterの長い方を使う。
- 探索窓は `max(90分, x + 30分)` とし、x増加時にも収集漏れを防ぐ。
- 現在のx・y・z、次回収集、休止終了、変更理由をSQLiteとメール末尾へ保存する。
- 403は認証・遮断の可能性を区別するため、x・yを学習変更せずy相当時間だけ安全休止する。
- zは初期20秒、最小10秒、最大120秒。
- 同一収集内で1本以上のクエリが成功した後に429が出た場合、間隔不足の証拠として `z = min(z + 5, 120)`。
- 最初のクエリで即429の場合はクエリ間隔を使っていないためzを変更しない。
- 24時間429がなく6回以上完全成功した場合、xの短縮と同時に `z = max(z - 1, 10)`。
- zは危険検知時+5秒、安全確認時-1秒とし、安全側へ大きく、危険側へ小さく動かす。

## x・y・zの個別安定域通知

- 直近3日間（72時間）、実収集12回以上を標本とする。
- 過去に429または429休止移行の観測がある場合だけ判定し、初期値のままという理由だけでは通知しない。
- x・y・zを個別に判定する。1項目だけ安定した場合も、その項目について通知する。
- ほぼ横ばい、または狭い範囲で2回以上方向反転して開始水準へ戻った場合を「安定域候補」とする。
- xは変動幅8分以内（横ばい判定2分以内）、yは1時間以内（0.5時間以内）、zは5秒以内（2秒以内）。
- 通知メールには安定した項目、その時点のx・y・z、中央値、変動幅、反転回数、観測期間を載せる。
- 同じ安定域では各項目につき一度だけ送る。基準値からxが8分超、yが1時間超、zが5秒超離れて再収束した場合は再通知する。
- 3項目すべてが安定した時点では、メール内で「全体安定」と明示する。
- 「理想値の確定」ではなく、次の仕組みを考えるための実運用上の安定候補として扱う。

## 影運用の合格条件

- 7日間、自動起動とDB記録が継続する。
- 429後に連続アクセスしない。
- 同一URLを新規記事として再計上しない。
- 0件と収集失敗を区別できる。
- 企業名の誤照合を標本確認し、許容範囲へ調整する。
- 底検知・特許モニターのタイマーとDBへ影響しない。

収集の健全性を確認しながら第2段階へ進み、候補標本を確認して第3段階を限定的に動かす。
AI判定の品質と費用上限を確認できるまで、第4段階のメールは有効化しない。

## コマンド

```bash
python gdelt_news_preflight.py
python gdelt_news_daily.py --max-queries 1
python monitorctl.py status gdelt-news
python monitorctl.py history gdelt-news --limit 10
```

メールの手動試験はSMTP設定済み環境でのみ行う。

```bash
GDELT_OPERATION_STAGE=4 python gdelt_news_daily.py --max-queries 0 --force-email
```

## 次の拡張

1. 優先50社の会社別検索を低頻度で追加する。
2. 451社を分割し、1日で一巡する補完検索を追加する。
3. TDnet、底検知、特許検知から優先会社リストを動的に作る。
4. ニュース後1・5・20・60営業日の株価反応を記録する。
5. 類似ニュースと株価反応分布による重要度補正を追加する。
